set -e
source /root/buildtools/harbourmasters/lib_build.sh

project_clone https://github.com/HarbourMasters/Ghostship.git

build_sdl2
build_libzip
build_json
build_tinyxml2
build_gsl
build_spdlog

project_configure_and_build GeneratePortO2R \
    -DUSE_OPENGLES=1 \
    -DUSE_NETWORKING=OFF \
    -DBUILD_CROWD_CONTROL=0 \
    -DCMAKE_BUILD_TYPE=${BUILD_TYPE:-Release} \
    -DCMAKE_EXE_LINKER_FLAGS="-ldl -pthread -fuse-ld=lld" \
    -DCMAKE_SHARED_LINKER_FLAGS="-fuse-ld=lld" \
    -DCMAKE_MODULE_LINKER_FLAGS="-fuse-ld=lld"

stage_libs Ghostship \
    "/usr/local/lib/libzip.so.5=libzip.so.5" \
    libtinyxml2.so.10 \
    "$PROJECT_DIR/libultraship/libtcc.so=libtcc.so"
