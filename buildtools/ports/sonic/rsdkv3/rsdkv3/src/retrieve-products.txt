#!/bin/bash
set -e

SRCDIR=$1
DESTDIR=$2

cd "$DESTDIR" || { echo "DESTDIR not found: $DESTDIR"; exit 1; }

cp "$SRCDIR/RSDKv3-Decompilation/build/RSDKv3" "$DESTDIR/RSDKv3"

rm -rf ./libs
cp -r "$SRCDIR/RSDKv3-Decompilation/build/libs" .

echo "Packaging complete."
